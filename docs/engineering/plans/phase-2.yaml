schema_version: 1
plan_version: 1
phase: Phase2
features:
  SEC-P1-003:
    title: "مراجعة صلاحيات 156 دالة"
    batches:
      - id: 1
        type: audit
        input: "docs/engineering/reports/verification-results.md"
        steps:
          - "راجع قائمة الـ 156 دالة."
          - "حدد أي دالة تحتاج أن تبقى للمستخدم العادي، وأيها تحتاج أن تكون للمدير فقط."
          - "أنشئ تقريراً بالتصنيفات."
        output: "docs/engineering/reports/SEC-P1-003-batch-1-classification.md"
        acceptance:
          - "Every one of the 156 functions appears in exactly one bucket."
          - "Buckets: KEEP_AUTHENTICATED, RESTRICT_ADMIN_ONLY, SERVICE_ROLE_ONLY, REVIEW_REQUIRED."
          - "No GRANT/REVOKE SQL applied in this batch."
          - "No source-code changes in this batch."
      - id: 2
        type: execute
        status: AWAITING_APPROVAL
        depends_on: [1]
        steps:
          - "After CTO review of Batch 1 report, generate REVOKE/guard SQL per bucket."
          - "Apply via migration tool."
          - "Re-run verification SELECT."
